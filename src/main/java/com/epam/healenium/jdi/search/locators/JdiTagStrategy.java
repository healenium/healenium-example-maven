package com.epam.healenium.jdi.search.locators;

import com.epam.healenium.jdi.search.JdiStrategy;

public class JdiTagStrategy implements JdiStrategy {
    @Override
    public boolean doAction(String selector) {
        return false;
    }
}
